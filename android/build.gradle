
buildscript {
        repositories {
          google()
          jcenter()
        }

        dependencies {
          classpath 'com.android.tools.build:gradle:3.5.3'
        }
}

apply plugin: 'com.android.library'

android {
    compileSdkVersion 28
    buildToolsVersion "28.0.3"

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 28
        versionCode 7
        versionName "1.0.7"
    }
    lintOptions {
        abortOnError false
    }
}

repositories {
    mavenCentral()
  google()
}

dependencies {
  api 'com.facebook.react:react-native:+'
  //compile fileTree(include: ['*.jar','*.aar'], dir: 'TMXProfiling-6.0-138')
  // compile fileTree(include: ['*.jar','*.aar'], dir: 'TMXProfilingConnections-6.0-138')
  // compile project(":TMXProfilingConnections-6.0-138")
  // compile project(":TMXProfiling-6.0-138")
  api fileTree(dir: 'TMXProfiling-6.0-138', include: ['*.aar'])
  api fileTree(dir: 'TMXProfilingConnections-6.0-138', include: ['*.aar'])
  // compile files('TMXProfiling-6.0-138/TMXProfiling-6.0-138.aar')
  // compile files('TMXProfilingConnections-6.0-138/TMXProfilingConnections-6.0-138.aar')
}
